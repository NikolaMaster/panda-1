@model PandaWebApp.FormModels.FeedbackForm

@using (Html.BeginForm("Create"))
{
    <div class="label">Загаловок</div>
    <div class="editor">@Html.TextBoxFor(m => m.Title)</div>
    
    <div class="label">Рейтинг</div>
    <div class="editor rating_editor">
        <div>
            @for (int i = 0; i < 4; i++)
            {
                <img src="/Content/img/light_gray_star.png" class="left red_stars" />
            }
            @for (int i = 0; i < 3;i++)
            {
                <img src="/Content/img/light_gray_star.png" class="left orange_stars" />
            }
            @for (int i = 0; i < 3;i++)
            {
                <img src="/Content/img/light_gray_star.png" class="left green_stars" />
            }
        </div>
        <div class="clear"></div>
        @Html.HiddenFor(x => x.Rating)
    </div>
    
    <div class="label">Отзыв</div>
    <div class="editor">@Html.EditorFor(m => m.Text, "Wysiwyg")</div>

    @Html.HiddenFor(x => x.AuthorId)
    @Html.HiddenFor(x => x.RecieverId)

    <input type="submit" value="Отправить" />
}
<script type="text/javascript"> initFeedbackCreate();</script>