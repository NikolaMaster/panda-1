@model PandaWebApp.FormModels.AlbumUnit

<div class="photo-album clear"  id="album_@Model.Id">
    <div class="album">
        <ul class="horiz">
            @{
                var index = 0;
                foreach (var item in Model.Photos)
                {
                    <li class="@(index == 0 ? "first" : "")">
                        <a data-lightbox="photo-promouter-set" href="@ImageCreator.Create(item.SourceUrl, WebConstants.PhotoHeight*2, WebConstants.PhotoWidth*2)">
                            <img src="@ImageCreator.Create(item.SourceUrl, WebConstants.PhotoHeight, WebConstants.PhotoWidth)"/>
                        </a>
                        <br/>
                        @Ajax.ActionLink("Удалить", "Delete", "Album", new { photoId = item.Id }, new AjaxOptions{ UpdateTargetId = "album_"+@Model.Id })
                        <br/>
                        @Html.ActionLink("Редактировать", "Edit", "Photo", new { id = item.Id }, new { })
                    </li>

                    if (index == WebConstants.PhotoPerRow)
                    {
                        index = 0;
                    }
                    else
                    {
                        index++;
                    }
                }
            }
        </ul>
    </div>
</div>
<div class="clear">
</div>